import styled from "styled-components";

export const CartStyles = styled.div`
   height: 4rem;
   position: relative;
   padding: 0 1rem;
   cursor: pointer;
`;

export const CartImgStyles = styled.img`
   height: 100%;
`;

export const CartItemsStyles = styled.div`
   position: absolute;
   top: 60%;
   left: 50%;
   translate: -50% -50%;
   font-size: 1.4rem;
`;